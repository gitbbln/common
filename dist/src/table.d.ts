import { TD } from "./common";
export declare class Table {
    map: TD;
    uns(u: number): string;
    us(code: number): string;
    pseudoSymUnicode(msg: any, list: any[], style: any, set: string): string;
}
